# Open in emacs using daemon
alias e "emacsclient -c --no-wait"

# Dtrx - always extract into a folder with same name
alias dtrx "dtrx --one=I"

set -xg EDITOR "emacsclient -c"
set -xg VISUAL "emacsclient -c"
set -xg TERMINAL "urxvt"
